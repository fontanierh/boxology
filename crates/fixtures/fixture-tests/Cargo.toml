[package]
name = "boxology-fixture-tests"
version = "0.0.0"
edition.workspace = true
publish = false

[dev-dependencies]
boxology-contract = { version = "=0.0.0", path = "../../boxology-contract" }
hello-contract = { version = "=0.0.0", path = "../hello/generated/contract", features = ["test-support"] }
