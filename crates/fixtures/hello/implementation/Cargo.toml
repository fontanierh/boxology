[package]
name = "hello-implementation"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
boxology-contract = { version = "=0.0.0", path = "../../../boxology-contract" }
hello-contract = { version = "=0.0.0", path = "../generated/contract" }
