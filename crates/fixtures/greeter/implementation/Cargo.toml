[package]
name = "greeter-implementation"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
boxology = { workspace = true }
boxology-contract = { workspace = true }
boxology-runtime = { workspace = true, features = ["test-support"] }
boxology_generated_contract = { package = "greeter-contract", version = "=0.0.0", path = "../generated/contract" }
