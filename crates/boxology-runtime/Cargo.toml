[package]
name = "boxology-runtime"
version = "0.0.0"
edition.workspace = true
publish = false

[features]
test-support = []

[dependencies]
boxology-contract = { version = "=0.0.0", path = "../boxology-contract" }
tokio-util = { version = "0.7.18", default-features = false, features = ["rt"] }
