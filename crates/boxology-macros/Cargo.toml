[package]
name = "boxology-macros"
version = "0.0.0"
edition.workspace = true
publish = false

[lib]
proc-macro = true

[dependencies]
boxology-contract-syntax = { version = "=0.0.0", path = "../boxology-contract-syntax" }
proc-macro2 = { version = "=1.0.107", default-features = false }
quote = "=1.0.47"
syn = { version = "=3.0.0", default-features = false, features = ["full", "parsing", "printing", "visit"] }
